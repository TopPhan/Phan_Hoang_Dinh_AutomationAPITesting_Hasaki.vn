# ================================================================
# ENV: dev — môi trường Development / Local
# ================================================================
# Chạy local:  mvn test (mặc định, không cần -Denv)
# Chạy CI:     mvn test -Denv=dev
# ================================================================

AUTHOR        = HoangDinh
ENV_NAME      = dev

# Credentials — để trống, override qua system property hoặc env var khi CI
USERNAME      =
PASSWORD      =
REMEMBER      = true

# Base URL — thường là staging hoặc local server khi dev
BASE_URI      = https://hasaki.vn/
BASE_PATH     =

# Device Fingerprinting (long-lived — safe to commit)
MOBILE.DEVICE.ID = 0dcc0f5b-f817-457e-b11b-8831301baf5f
BROWSER.UUID     = daaf2794-28c0-469d-b135-592ae646736e
HSK.CDP.UID      = %2225a98b61-a485-4ce6-a940-58ced2a512bc%22
HSK.CDP.CID      = 752508a1eb9439c2347cec68680ff7ebdd477fb008d8c31f6addc45230cde04e

# Timeout settings (ms)
CONNECTION.TIMEOUT = 10000
READ.TIMEOUT       = 15000
